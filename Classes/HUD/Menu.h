#pragma once

class Menu
{
    public:
        Menu();
        virtual ~Menu();

        // Function
        void draw(sf::RenderWindow* window);
        void update(sf::RenderWindow* window);
       

    protected:
    private:
        // Function
};
